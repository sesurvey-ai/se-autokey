# credentials
.env

# Python
__pycache__/
*.pyc

# log การรัน/ทดสอบ (เฉพาะเครื่อง)
*.log

# bundled portable runtime (rebuild with build-runtime.bat — don't commit, ~250MB)
runtime/

# ข้อมูลระหว่างรัน
downloaded_images/
runs/
# โฟลเดอร์วาง zip export (fallback หมวดรูป) — ไม่ commit ไฟล์ที่วาง แต่เก็บ README ไว้
zip_import/*
!zip_import/README.txt

# ไฟล์งานเฉพาะเครื่อง
*.zip
SURV_REPORT_*.txt

# ข้อมูลธุรกิจ/พนักงาน + ไฟล์ scratch (อาจมีข้อมูลส่วนตัว/รหัส) — ห้าม commit
*.xlsx
New*.txt
